name: Gradle Minecraft build

on:
  workflow_call:
    inputs:
      java-version:
        description: "JDK version to use"
        required: true
        type: string
      java-distribution:
        description: "JDK distribution to use"
        required: false
        type: string
        default: "temurin"
      runs-on:
        description: "Runner to use"
        required: false
        type: string
        default: "ubuntu-latest"
      environment:
        description: "Environment to use"
        required: false
        type: string
      artifact-archive:
        description: "Whether to compress the artifact"
        required: false
        type: boolean
        default: false
      artifact-path:
        description: "Path of the artifacts to get"
        required: false
        type: string
        default: "build/libs/*.jar"

permissions:
  contents: write

jobs:
  build:
    name: Gradle - Build
    runs-on: [ "${{ inputs.runs-on }}" ]
    environment: ${{ inputs.environment }}
    concurrency:
      group: gradle-build-${{ github.ref }}
      cancel-in-progress: true
    steps:
      - name: Checkout source
        uses: actions/checkout@v6.0.2
      - name: Set up JDK
        uses: actions/setup-java@v5.2.0
        with:
          distribution: ${{ inputs.java-distribution }}
          java-version: ${{ inputs.java-version }}

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v6.0.0
      - name: Build
        run: |
          ./gradlew \
          build

      - name: Upload mod file
        uses: actions/upload-artifact@v7.0.0
        with:
          name: mod-file
          path: ${{ inputs.artifact-path }}
          if-no-files-found: error
          retention-days: 7
          archive: ${{ inputs.artifact-archive }}
